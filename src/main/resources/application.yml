server:
  port: 8080

spring:
  http:
    encoding:
      charset: UTF-8
      enabled: true

  datasource:
    platform: h2

  jpa:
    show_sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect